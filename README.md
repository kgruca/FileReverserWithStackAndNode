# FileReverserWithStackAndNode
This was a class project to demonstrate the reversal of contents in a file using a generic stack data structure. 
I created a class, which would be used on both Strings and floats, and tested this reverser class in main, with custom files. 
